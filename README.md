# Assessment Implementation

This is a personal implementation based on the requirements about designing a smart meter aggregation system. I will update the README subsequently with the breakdown of the requirements.

## Requirements
- To run the app, you would need Docker installed locally.
- Run the command `docker-compose up -d` from the root folder